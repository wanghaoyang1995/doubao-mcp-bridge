{
  "mcpServers": {
    "anki-mcp": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@ankimcp/anki-mcp-server", "--stdio", "--read-only"],
      "env": {
        "ANKI_CONNECT_URL": "http://localhost:8765"
      }
    }
  }
}